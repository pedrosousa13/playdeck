# Backpack parity matrix

One row per story in EF Backpack's three video story files — `Video.stories.tsx`,
`AutoplayVideo.stories.tsx` and `VideoHoverPreview.stories.tsx` — recording
whether the `BackpackVideo` wrapper in `apps/storybook` reproduces it on Reely
primitives. The wrapper reimplements Backpack's `VideoPlayer` prop API so that
Backpack's own story args copy across near-verbatim; it exists to prove the swap
is possible, not to ship, so parity here means behavior, not pixels.

- `full` — a Reely story reproduces the Backpack story with the same args.
- `partial` — a Reely story exists, but something the Backpack story shows is
  not reproduced. The note says what.
- `gap` — no Reely story yet. The note names the one issue that closes it.

Counts: 38 rows — 2 `full`, 2 `partial`, 34 `gap`.

A converted row means the wrapper attaches a real provider for it, not that it
renders under the workbench's mock. `Backpack parity/Video` covers behavior
deterministically against the mock; `Real playback/BackpackVideo` is where the
provider actually attaches.

## Video.stories.tsx

| Backpack story                          | Reely story                            | Status    | Notes                                                                                                                                                                                                                                                                                                                |
| --------------------------------------- | -------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Default`                               | `Backpack parity/Video → Default`      | `full`    | Vimeo `vimeo.com/336066147`, muted. The wrapper passes `url` through and Reely's source detection picks the provider; `Real playback/BackpackVideo → Vimeo` attaches it for real.                                                                                                                                    |
| `DefaultThemeConfig`                    | —                                      | `gap`     | SIDEPRO-202 renders the wrapper's own default style object as JSON in its place. Backpack dumps `videoStyles` from its styling stack, which the wrapper does not reproduce, so the contents will differ.                                                                                                             |
| `Light`                                 | —                                      | `gap`     | SIDEPRO-199 — `light` presents the provider's fetched thumbnail instead of mounting the provider.                                                                                                                                                                                                                    |
| `WithoutHoverEffect`                    | —                                      | `gap`     | SIDEPRO-199 — `hoverEffect={false}` disables the cover's zoom on hover.                                                                                                                                                                                                                                              |
| `YouTubeVideo`                          | `Backpack parity/Video → YouTubeVideo` | `full`    | `youtube.com/watch?v=mhN3E_hlWmU`, muted. `Real playback/BackpackVideo → YouTube` attaches the provider and plays.                                                                                                                                                                                                   |
| `YouTubeShortsVideo`                    | —                                      | `gap`     | SIDEPRO-202 — `aspectRatios: '9/16'` over a Shorts URL.                                                                                                                                                                                                                                                              |
| `YouTubeShortsVideoAndCustomCoverImage` | —                                      | `gap`     | SIDEPRO-199 — `placeholderImageSrc` plus `alt` over a Shorts source. Its `aspectRatios: '9/16'` arrives with SIDEPRO-202.                                                                                                                                                                                            |
| `WistiaVideo`                           | —                                      | `gap`     | SIDEPRO-205 — the Wistia provider SIDEPRO-198 built is in place; only the wrapper story is missing.                                                                                                                                                                                                                  |
| `WistiaWithPlayerConfig`                | —                                      | `gap`     | SIDEPRO-205 — `playerConfig.wistia` passthrough for `swatch` and `playerColor`.                                                                                                                                                                                                                                      |
| `WithControls`                          | `Backpack parity/Video → WithControls` | `partial` | `controls: true` replaces the wrapper's click-to-toggle surface, which matches. What differs is the chrome: Backpack forwards `controls` to react-player and gets the provider's own controls, while the wrapper renders Reely's `Player.Controls`, because Reely renders its controls itself.                       |
| `Loop`                                  | `Backpack parity/Video → Loop`         | `partial` | `controls: true`, `loop: true`. `loop` reaches `Player.Root` but never the provider: Reely forwards its native playback options to the HLS and native branches only, so a Vimeo or YouTube source is unaffected and the video does not loop. Tracked as SIDEPRO-210; this row converts under SIDEPRO-197 regardless. |
| `VimeoCoverImage`                       | —                                      | `gap`     | SIDEPRO-199 — `light` against Vimeo's own thumbnail.                                                                                                                                                                                                                                                                 |
| `YouTubeCoverImage`                     | —                                      | `gap`     | SIDEPRO-199 — `light` against YouTube's own thumbnail.                                                                                                                                                                                                                                                               |
| `CustomCoverImage`                      | —                                      | `gap`     | SIDEPRO-199 — `placeholderImageSrc` wins over the fetched Vimeo thumbnail.                                                                                                                                                                                                                                           |
| `CustomCoverImageYouTube`               | —                                      | `gap`     | SIDEPRO-199 — the same, over YouTube.                                                                                                                                                                                                                                                                                |
| `WithShadowVariant`                     | —                                      | `gap`     | SIDEPRO-202 — `variant: 'shadow-m'`, approximated with story-local CSS rather than Backpack's tokens.                                                                                                                                                                                                                |
| `WithOutlineVariant`                    | —                                      | `gap`     | SIDEPRO-202 — `variant: 'outline'`, approximated with story-local CSS.                                                                                                                                                                                                                                               |
| `WithXLSizePlayIcon`                    | —                                      | `gap`     | SIDEPRO-202 — `playIconSize: 'xl'`.                                                                                                                                                                                                                                                                                  |
| `WithRenderCustomImage`                 | —                                      | `gap`     | SIDEPRO-199 — `renderCustomImage` renders the cover through a consumer's own element type, receiving the resolved src and alt.                                                                                                                                                                                       |
| `WithEvents`                            | —                                      | `gap`     | SIDEPRO-201 — the wrapper already fires `onPlayChange`, but the story lands with the external-control work that makes it report externally caused transitions too.                                                                                                                                                   |
| `InPage`                                | —                                      | `gap`     | SIDEPRO-200 — full-screen scroll layout with `pauseOnOutOfViewport` on.                                                                                                                                                                                                                                              |
| `WithoutPauseOnOutOfViewport`           | —                                      | `gap`     | SIDEPRO-200 — unmuted, `pauseOnOutOfViewport: false`.                                                                                                                                                                                                                                                                |
| `WithPauseOnOutOfViewport`              | —                                      | `gap`     | SIDEPRO-200 — unmuted, `pauseOnOutOfViewport: true`.                                                                                                                                                                                                                                                                 |
| `WithThemeConfig`                       | —                                      | `gap`     | SIDEPRO-202 adds a theme-config-shaped escape hatch that overrides the `outline` variant. Backpack's args are Tailwind classes from its own stack; the wrapper's override is story-local CSS, so the story reads differently even once converted.                                                                    |
| `SocialCarouselAtomIntegration`         | —                                      | `gap`     | SIDEPRO-201 — three buttons drive one player: play, pause, release control. Backpack coordinates through a jotai atom; the wrapper uses Reely's command API, so the mechanism differs while the behavior matches.                                                                                                    |

## AutoplayVideo.stories.tsx

| Backpack story               | Reely story | Status | Notes                                                                                                   |
| ---------------------------- | ----------- | ------ | ------------------------------------------------------------------------------------------------------- |
| `Default`                    | —           | `gap`  | SIDEPRO-203 — a `BackpackAutoplayVideo` composition that starts muted playback on viewport entry.       |
| `WithCustomPlaceholderImage` | —           | `gap`  | SIDEPRO-203 — `placeholderImageSrc` shows until autoplay starts.                                        |
| `WistiaVideo`                | —           | `gap`  | SIDEPRO-205 — Wistia autoplay through `playerConfig.wistia`. The provider from SIDEPRO-198 is in place. |
| `InPage`                     | —           | `gap`  | SIDEPRO-203 — the scroll-container decorator that shows entry and exit.                                 |

## VideoHoverPreview.stories.tsx

| Backpack story               | Reely story | Status | Notes                                                                                                                                                |
| ---------------------------- | ----------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Default`                    | —           | `gap`  | SIDEPRO-204 — a `BackpackVideoHoverPreview` composition: cover at rest, muted preview while hovered.                                                 |
| `DefaultThemeConfig`         | —           | `gap`  | SIDEPRO-204 converts the JSON dump alongside the rest. As with `Video`'s own copy, the object dumped is the wrapper's, not Backpack's `videoStyles`. |
| `WithCustomDuration`         | —           | `gap`  | SIDEPRO-204 — `duration: 3` sets the preview window; 3 seconds is also Backpack's default.                                                           |
| `WithPlayIcon`               | —           | `gap`  | SIDEPRO-204 — `showPlayIcon: true`.                                                                                                                  |
| `WithSound`                  | —           | `gap`  | SIDEPRO-204 — `muted: false`.                                                                                                                        |
| `WithoutLoop`                | —           | `gap`  | SIDEPRO-204 — `loop: false` stops the preview looping back to the start.                                                                             |
| `WithCustomPlaceholderImage` | —           | `gap`  | SIDEPRO-204 — reuses the cover-image handling SIDEPRO-199 builds for the wrapper.                                                                    |
| `WithRenderCustomImage`      | —           | `gap`  | SIDEPRO-204 — `renderCustomImage` over the preview's cover.                                                                                          |
| `WithCustomAspectRatio`      | —           | `gap`  | SIDEPRO-204 — `aspectRatios: '1/1'`, on the ratio support SIDEPRO-202 adds.                                                                          |
