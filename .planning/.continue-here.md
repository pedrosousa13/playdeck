---
context: default
next_issue: 15
status: 34-merged
last_updated: 2026-07-24T19:45:00Z
---

# Continue Here — Reely

_Last updated: 2026-07-24, after #34 merged._

## State

- **main = `20e57d7`** — clean, synced local+remote. Working tree clean. Branch deleted.
- **Just shipped:** #34 error primitive — PR #55 squash-merged, issue CLOSED, all CI green.
- **#34 built:** `Player.ErrorDisplay` in `packages/react/src/index.tsx`. Unblocks #9 (with #15).
- Plan: do #34+#15 both (the two remaining #9 blockers) then #9. Serial — both share `packages/react`. #34 done, **#15 next**.
- Prior: #31 control primitives PR #54 merged, CLOSED. Full SDD ledger: `.superpowers/sdd/progress.md`.

## What #34 added (`packages/react`)

- **ErrorDisplay** (in `index.tsx`): headless error surface from `PlayerState.error`. `role="alert"`, `data-reely-part="error"`/`data-state`=category/`data-provider`, className/style/ref passthrough.
- **Capability-aware retry**: present only when `error.recoverable` (the state-level "provider offers retry" signal) — absent, never disabled-visible.
- **Replaceable children**: render-prop `({ error, retry }) => ReactNode`; `retry` is `null` when not recoverable (keeps custom renderers capability-aware).
- Tests `test/error-display.test.tsx` (7). Stories `error-display.stories.tsx` (Retryable/NotRecoverable/CustomRendering). Registered `error`/`error-message`/`error-retry` parts in `Contract.mdx`.

## What #31 added (`packages/react`)

- **Icons** (`src/icons.tsx`, re-exported from `index.tsx`): 14 inline-SVG named exports (`PlayIcon`…`ReplayIcon`), `currentColor`, tree-shakeable.
- **SettingsMenu** (in `index.tsx`): `SettingsMenu` / `SettingsMenuTrigger` / `SettingsMenuContent` / `MenuItem` / `MenuRadioGroup` / `MenuRadioItem`. Roving focus, Escape/select restore focus to trigger, `role="menu"`+`data-reely-menu="open"` (inherits #8 shortcut suppression).
- **Gestures** (in `index.tsx`): headless full-bleed layer. `onToggleControls` (single tap, never playback), removable double-tap seek (`doubleTapSeek`, `seekOffset`), `onSeek`. **Emits callbacks only — holds NO visibility state (that's #9's job).**

## Next up (recommended order)

1. **After #55 merges:** `git checkout main && git pull`, delete `feat/34-error-display`.
2. **#15 captions** (native/HLS hybrid, `ready-for-agent` + `needs-human`) — second #9 blocker. Shares `packages/react`. Note: after #14 per original dep; check #14 state first. HITL points expected.
3. **Then #9** preset layout / auto-hide / responsive — now unblocked (both #34+#15 done). Consumes `Gestures onToggleControls` + composes ErrorDisplay + captions.
4. Later: #10 theme CSS, #35 buffering, #16, #32, #28 → #18 release.
   - Overlap watch: don't run two `packages/react` issues concurrently.

## Open follow-ups / HITL (needs human)

- **#31 visual review** — drive Storybook dev @ localhost:6006 for real look of `settings-menu`/`gestures` stories (play-tests pass ≠ visually verified; memory `verify-storybook-visually`).
- **#31 deferred minor** — menu-button `ArrowUp` focuses first item, should focus last (APG). File follow-up or leave. `SettingsMenuTrigger` onKeyDown / `SettingsMenuContent` autofocus in `packages/react/src/index.tsx`.
- **PR #29** — pre-existing dup of #19, superseded by merged #40. Close as superseded?
- **Device matrices** (pre-#31, open): #17 fullscreen/PiP; #30 AirPlay + lock-screen Media Session.
- **Real-playback verifications**: #11 YouTube, #12 Vimeo, #13 HLS-Safari.
- Filed follow-ups: #48 (CI build-once cross-job), #49 (polished Storybook).

## Process notes (this repo)

- **CI `static` = `format:check && lint && typecheck`.** Prettier is separate from eslint. Local verification MUST run `pnpm format:check` too — eslint-green ≠ prettier-clean (bit us on #54's first CI run). Memory: `verify-includes-format-check`.
- pnpm 11: use `pnpm exec vitest`/`pnpm exec playwright --grep X`, not `pnpm test -- --grep`.
- Gates: `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm --filter @reely/react test`, `pnpm --filter @reely/bundle-native-only test`, `pnpm --filter @reely/storybook test` (real chromium).
- Branch protection ON (enforce_admins), required: static/unit/e2e×3/package/storybook/hls-native-webkit/hls-paths. Ship via PR + squash auto-merge (`gh pr merge N --squash --auto`).
- SDD flow: brainstorm → spec (`docs/superpowers/specs/`) → plan (`docs/superpowers/plans/`, gitignored) → per-task implementer→review→fix → final whole-branch review.
