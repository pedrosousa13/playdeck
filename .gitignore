node_modules/
dist/
.worktrees/
storybook-static/
*storybook.log
playwright-report/
test-results/
*.tsbuildinfo
.turbo/
.scratch/
.factory/journal.json
# main's copy of scripts/, fetched by the `audit` job in .github/workflows/ci.yml
# and run from there. Never committed: a copy that lived in the tree would be
# editable by the pull request it judges, which is the whole point of it.
.gate/
